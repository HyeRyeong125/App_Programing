/**
 * 상식 퀴즈 챌린지 - 순위 시스템 (3단계에서 구현)
 */
const Ranking = (() => {
  function save(data) {
    // 3단계에서 구현
  }

  function render() {
    // 3단계에서 구현
    const container = document.getElementById('ranking-table-container');
    container.innerHTML = '<p class="empty-ranking">아직 기록이 없습니다.</p>';
  }

  return { save, render };
})();
